# SQLite database path (defaults to "arcane-forge.db" if not set)
DATABASE_URL=arcane-forge.db

# MongoDB Atlas connection (optional — leave blank to disable cloud sync)
MONGODB_URI=
MONGODB_DB_NAME=arcane_forge
